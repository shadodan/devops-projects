#!/bin/bash

# Export command exports the variable to child shells
echo "The $SEASON is more than expected, this time"

# To make the variable permanent to a user you need to add it to .bashrc or .bash_profile of the user
# To make the variable permanent globally you need to modify the /etc/profile file adding the export in it
